# Workers

Background jobs that materialize counters and analytics:
- `UserStats` updates
- `PostMetrics` hotScore recomputation
- `CategoryOverallReview` aggregation
- `LeaderboardSnapshot` generation
- Survey campaign pacing/accounting

