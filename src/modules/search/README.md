# Search Module

Responsibilities:
- Category search (`GET /search/categories`)
- (Later) template discovery, user-generated tier lists, trending categories

Key tables:
- `Category`, `TierListTemplate`

